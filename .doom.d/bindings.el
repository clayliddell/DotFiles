;;; ~/.doom.d/bindings.el -*- lexical-binding: t; -*-

;; This file is for storing custom emacs keybindings. For example, if you wanted
;; to bind the key SPC-o-v to running the `vterm' command

(global-set-key (kbd "<tab>") 'indent-for-tab-command)
